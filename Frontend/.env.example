# ============ LeafAI 前端环境变量示例 ============
# 开发环境读取 .env.development，生产构建读取 .env.production。
# 留空则代码兜底为 http://localhost:8000。

# 后端 API 基地址
#  - 开发：默认 http://localhost:8000（直连本地后端，无需配置）
#  - 生产：/api（走反向代理，由 Nginx/Caddy 转发到后端）
VITE_API_BASE_URL=http://localhost:8000